<h1><%= @artist.name %></h1>
<p><%= @artist.bio %></p>

<% @artists.each do |artist| %>
  <div><%= link_to artist.genre, artist_path(artist) %></div>
  <div><%= link_to artist.songs, artist_path(artist) %></div>
<% end %>
